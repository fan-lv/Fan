if (theme.comments.enable)
    if (theme.comments.type === 'gitment')
        include ./gitment.pug
    else if (theme.comments.type === 'valine')
        include ./valine.pug
    else if (theme.comments.type === 'twikoo')
        include ./twikoo.pug
